uid: 476d891e-b4e2-4628-8962-cee32dabe89d
info:
  name: VALIDATION accounts  (Organisation service)
  type: http
  seq: 2
http:
  method: GET
  url: '{{organisationServiceURL}}/accounts/{{provisionAccountResponseAccountId}}'
  headers:
  - name: Authorization
    value: Bearer {{Jwt}}
runtime:
  scripts:
  - type: after-response
    code: |-
      test("OK", function () {
          expect(res.getStatus()).to.equal(200);
      });
