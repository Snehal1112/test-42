uid: 8906c87a-bd1e-40a4-9a50-0e59f7377a20
info:
  name: VALIDATION product instances  (Organisation service)
  type: http
  seq: 3
http:
  method: GET
  url: '{{organisationServiceURL}}/accounts/{{provisionAccountResponseAccountId}}/product-instances'
  headers:
  - name: Authorization
    value: Bearer {{Jwt}}
runtime:
  scripts:
  - type: after-response
    code: |-
      test("OK", function () {
          expect(res.getStatus()).to.equal(200);
      });
